{
  "name": "screenshot-extension",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --config vite.config.background.ts && vite build --config vite.config.content.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "prettier": "^3.5.3",
    "typescript": "^5.0.2",
    "vite": "^6.3.5"
  }
}
